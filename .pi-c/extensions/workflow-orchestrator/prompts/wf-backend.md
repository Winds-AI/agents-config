---
description: Run backend API-only workflow mode before execution
---

Before doing anything else, call tool `workflow_set_mode` with:
- `target`: `backend`

Then execute the request:
$@

Use only backend API discovery/call workflow constraints.
